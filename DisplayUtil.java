public class DisplayUtil {

    public void displayGraph(Graph graph) {
        // Placeholder for graphical display logic
        System.out.println("Displaying graph with " + graph.getNodes().size() + " nodes.");
    }


    public static void displayGraphText(Graph graph) {

        // for (Node node : graph.getNodes()) {
        //     System.out.println("Node ID: " + node.getId() + ", Label: " + node.getLabel());
        // }
        // for (Node node : graph.getNodes()) {
        //     for (Link link : node.getLinks()) {
        //         System.out.println("Link from " + link.getSource().getId() + " to " + link.getTarget().getId());
        //     }
        // }
    }

}
